{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Test Harness",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8181",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start Test Harness"
        }
    ]
}
